li $a1, 5  #Here you set $a1 to the max bound.
li $v0, 42  #Generates the random number.
syscall

move $t1, $a0

li $v0, 1
move $a0, $t1
syscall

li $a1, 5  #Here you set $a1 to the max bound.
li $v0, 42  #Generates the random number.
syscall

move $t1, $a0

li $v0, 1
move $a0, $t1
syscall

li $a1, 5  #Here you set $a1 to the max bound.
li $v0, 42  #Generates the random number.
syscall

move $t1, $a0

li $v0, 1
move $a0, $t1
syscall

li $a1, 5  #Here you set $a1 to the max bound.
li $v0, 42  #Generates the random number.
syscall

move $t1, $a0

li $v0, 1
move $a0, $t1
syscall

li $a1, 5  #Here you set $a1 to the max bound.
li $v0, 42  #Generates the random number.
syscall

move $t1, $a0

li $v0, 1
move $a0, $t1
syscall
